# calendar
“My project is an AI-assisted automatic timetable generator designed for schools and colleges. Instead of manually scheduling teachers, classes, and rooms—which often creates conflicts—the system uses simple machine-learning ideas to detect clashes, balance workloads, and predict conflict-prone time slots based on past data.
